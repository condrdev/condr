# CI runs `buf lint` and `buf breaking` (ADR 0028). Only the binary encoding reaches peers
# and the Session Snapshot, so breaking checks wire compatibility, not names.
version: v2
modules:
  - path: .
lint:
  use:
    - STANDARD
breaking:
  use:
    - WIRE
